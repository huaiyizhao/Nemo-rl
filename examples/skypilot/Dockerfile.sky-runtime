FROM nvcr.io/nvidia/nemo-rl:v0.6.0

USER root

# SkyPilot's bootstrap runs before task-level pod overrides are applied. Keep
# its uv invocations from loading NeMo-RL's setuptools>=80 project override.
ENV UV_NO_CONFIG=1

WORKDIR /root
